# Tauri desktop wrapper — never deployed to web
src-tauri/

# Prisma client — regenerated by `prisma generate` in vercel.json build command
src/generated/prisma/

# Documentation and planning artifacts
docs/
graphify-out/

# Tests — not needed at runtime
tests/

# Reference PDFs in repo root — never served by Next.js
/*.pdf

# Playwright / test artifacts (gitignored normally, defensive)
playwright-report/
test-results/
.playwright-mcp/
// Real implementation comes in Phase 2. This skeleton compiles so CI can build it.
// serde_json, serde, tokio, dirs — wired in Phase 2 (UDP discovery / payload deserialization)
#![allow(unused_crate_dependencies)]

fn main() {
    println!("metalu-client 0.2.0 — skeleton, not yet implemented");
}

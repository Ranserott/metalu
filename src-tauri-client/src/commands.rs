// Placeholder for Tauri commands (e.g., navigate_to_url).
// Future expansion: explicit Tauri command invoke handlers if the webview
// needs to request the server URL at runtime. For v1, the URL is baked
// into the WebviewWindow at startup, so no commands are needed yet.